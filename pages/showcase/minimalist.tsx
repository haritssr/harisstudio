import { Title, Topic } from '@/components/DesignSystem';
import Layout from '@/components/Layout';

export default function About() {
  return (
    <Layout browserTitle='Minimalist' description='Minimalist'>
      <Title name='Minimalist' />
    </Layout>
  );
}
