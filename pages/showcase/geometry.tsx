import { Title, Topic } from '@/components/DesignSystem';
import Layout from '@/components/Layout';

export default function About() {
  return (
    <Layout browserTitle='Geometry' description='Geometry'>
      <Title name='Geometry' />
    </Layout>
  );
}
