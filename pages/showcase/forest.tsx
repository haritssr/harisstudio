import { Title, Topic } from '@/components/DesignSystem';
import Layout from '@/components/Layout';

export default function About() {
  return (
    <Layout browserTitle='Forest' description='Forest'>
      <Title name='Forest' />
    </Layout>
  );
}
